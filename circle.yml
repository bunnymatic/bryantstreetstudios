machine:
  environment:
    DATABASE_URL: postgres://ubuntu:@127.0.0.1:5432/bryant_test
database:
  override:
    - psql -c "create database bryant_test"
test:
  override:
    - bundle exec rake spec
